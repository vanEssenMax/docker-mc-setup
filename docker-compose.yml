version: '3.5'

services:
  ls:
    image: itzg/minecraft-server
    environment:
      EULA: "TRUE"
      TYPE: "BUKKIT"
      ONLINE_MODE: "FALSE"
    volumes:
      - /data/lobbyserver
    restart: always
  ms:
    image: itzg/minecraft-server
    environment:
      EULA: "TRUE"
      MEMORY: "3G"
      ONLINE_MODE: "FALSE"
    volumes:
      - /data/mainserver
    restart: always
  bungeecord:
    image: itzg/bungeecord
    environment:
      BUNGEE_JAR_REVISION: "1"
    ports:
      - 25565:25577
    volumes:
      - ./config.yml:/config/config.yml
